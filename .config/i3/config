# Default Settingzas {{{
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $alt Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango: System San Francisco Display 11

new_window pixel 3
new_float pixel 3

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec terminator

# kill focused window
bindsym $mod+Shift+q kill

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -f System San Francisco Display 14 -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+semicolon split h; exec notify-send -t 1000 'Tile Horizontal'


# split in vertical orientation
bindsym $mod+v split v; exec notify-send -t 1000 'Tile Vertical'


# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking; exec notify-send -t 1000 'Layout Stacking'
bindsym $mod+t layout tabbed; exec notify-send -t 1000 'Layout Tabbed'
bindsym $mod+e layout toggle split; exec notify-send -t 1000 'Layout Split'


# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container

#bindsym $mod+d focus child
#}}}

# Workspace Config {{{
set $workspace1 "1:  Android Studio"
set $workspace2 "2:  Chrome"
set $workspace3 "3:  Slack"
set $workspace4 "4:  Terminals"
set $workspace5 "5:  IntelliJ"
set $workspace6 "6:  Calibre"
set $workspace7 "7:  Git"
set $workspace8 "8:"
set $workspace9 "9:"
set $workspace10 "10:"
set $workspace11 "11:"
set $workspace12 "12:"

set $bottom "DP-2"
set $top    "DP-0"
set $left   "HDMI-0"
set $right  "HDMI-1"

workspace $workspace1 output $bottom
workspace $workspace2 output $top
workspace $workspace3 output $left
workspace $workspace4 output $right
workspace $workspace5 output $bottom
workspace $workspace6 output $top
workspace $workspace7 output $top
workspace $workspace8 output $right
workspace $workspace9 output $bottom
workspace $workspace10 output $top
workspace $workspace11 output $left
workspace $workspace12 output $right

# switch to workspace
bindsym $mod+1 workspace $workspace1
bindsym $mod+2 workspace $workspace2
bindsym $mod+3 workspace $workspace3
bindsym $mod+4 workspace $workspace4
bindsym $mod+5 workspace $workspace5
bindsym $mod+6 workspace $workspace6
bindsym $mod+7 workspace $workspace7
bindsym $mod+8 workspace $workspace8
bindsym $mod+9 workspace $workspace9
bindsym $mod+0 workspace $workspace10

# TODO: Hotkeys for workspaces 11 + 12
# bindsym $mod+ workspace $workspace11
# bindsym $mod+ workspace $workspace12

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspace1
bindsym $mod+Shift+2 move container to workspace $workspace2
bindsym $mod+Shift+3 move container to workspace $workspace3
bindsym $mod+Shift+4 move container to workspace $workspace4
bindsym $mod+Shift+5 move container to workspace $workspace5
bindsym $mod+Shift+6 move container to workspace $workspace6
bindsym $mod+Shift+7 move container to workspace $workspace7
bindsym $mod+Shift+8 move container to workspace $workspace8
bindsym $mod+Shift+9 move container to workspace $workspace9
bindsym $mod+Shift+0 move container to workspace $workspace10

#}}}

# Rofi Settings {{{
#bindsym $mod+d exec rofi -show run -eh 2 -width 100 -padding 100 -opacity "80" -bw 0 -hlfg "#9575cd" -font "System San Francisco Display 14"
bindsym $mod+d exec i3-dmenu-desktop --dmenu='rofi -i -dmenu -m -4 -location 0 -show run -lines 8 -bw 0 -font "System San Francisco Display 14" -padding 50 -no-disable-history'

# }}}

# Misc. {{{
bindsym $mod+w exec --no-startup-id ~/bin/quickswitch.py

bindsym $mod+c exec --no-startup-id chromium-browser

#popup_during_fullscreen leave_fullscreen
workspace_auto_back_and_forth yes
focus_follows_mouse yes

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show

force_display_urgency_hint 1000 ms

bindsym $mod+Print exec "shutter -s"

# next/previous workspace
bindsym $mod+Tab workspace next
bindsym $mod+Shift+Tab workspace prev

# }}}

# Modes {{{

# Resize window mode {{{
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 5 px or 5 ppt
        bindsym Down resize grow height 5 px or 5 ppt
        bindsym Up resize shrink height 5 px or 5 ppt
        bindsym Right resize grow width 5 px or 5 ppt
	
	bindsym Shift+Left resize shrink width 1px or 1 ppt
       	bindsym Shift+Down resize grow width 1px or 1 ppt
	bindsym Shift+Up resize shrink width 1px or 1 ppt
       	bindsym Shift+Right resize grow width 1px or 1 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# }}}

# System Mode {{{
# Press Ctrl+Alt+Delete will show log out menu
set $locker i3lock && sleep 1
set $mode_system System (l) lock, (e) logout, (r) reboot, (s) shutdown
mode "$mode_system" {
        bindsym l exec --no-startup-id $Locker, mode "default"
        bindsym e exec --no-startup-id i3-msg exit, mode "default"
        bindsym r exec --no-startup-id systemctl reboot, mode "default"
        bindsym s exec --no-startup-id systemctl poweroff -i, mode "default"
        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $alt+Control+Delete mode "$mode_system"

# }}}

###}}}

# Window Colors {{{
set $black               #000000
set $bg-color 	         #4286f4
set $inactive-bg-color   #ffff99
set $text-color          #f3f4f5
set $inactive-text-color #676E7D
set $urgent-bg-color     #E53935

# window colors
#                          border                background            text                     indicator
client.focused              $black                $bg-color             $text-color              #e53935
client.unfocused            $black                $inactive-bg-color    $inactive-text-color     #e53935
client.focused_inactive     $black                $inactive-bg-color    $inactive-text-color     #e53935
client.urgent               $urgent-bg-color      $urgent-bg-color      $text-color              #e53935
# }}}

# I3 Status Bar {{{
bar {
	output "DP-0"
	output "DP-2" 
  	status_command i3status
        font pango: System San Francisco Display 12
	colors {
		background #2f343f
        separator #757575
		statusline #839496

		#                  border             background         text
		focused_workspace  $bg-color          $bg-color          $text-color
		inactive_workspace $inactive-bg-color $inactive-bg-color $inactive-text-color
		urgent_workspace   $urgent-bg-color   $urgent-bg-color   $text-color
	}
}

# bar
bar {
        output "HDMI-0"
        output "HDMI-1"
	tray_output "HDMI-0"
        tray_output "HDMI-1"
        font pango: System San Francisco Display 11
        status_command i3status
        colors {
                background #2f343f
                separator #757575
                statusline #839496
                #                  border             background         text
                focused_workspace  $bg-color          $bg-color          $text-color
                inactive_workspace $inactive-bg-color $inactive-bg-color $inactive-text-color
                urgent_workspace   $urgent-bg-color   $urgent-bg-color   $text-color
        }
}

# }}}

# Window Assignments + Rules {{{

# Studio
#for_window [class="jetbrains-studio" instance="sun-awt-X11-XFramePeer"] move container to workspace $workspace1
#for_window [class="jetbrains-studio" instance="sun-awt-X11-XDialogPeer"] move container to workspace $workspace1

for_window [class="jetbrains-studio"] boarder none, move container to workspace $workspace1, $workspace1
#for_window [title="Settings"] move container to workspace $workspace1, focus
#for_window [class="jetbrains-studio" floating] move absolute position center
#for_window [class="jetbrains-studio" title="Project"] resize set 615 1413, move left, floating disable
#for_window [class="jetbrains-studio"] border none


# Replace in Path
# Select Methods to Override/Implement
# Select Methods to Implement

# Git GUI
for_window [class="Git-gui"] move container to workspace $workspace2, fullscreen enable, focus

# Genymotion
for_window [class="Genymotion Player"] move container to workspace $workspace4

# Hangouts
for_window [class="^Chromium-browser$" instance="^crx_knipolnnllmklapflnccelgolnpehhpl$"] floating disable
assign [class="^Chromium-browser$" instance="^crx_knipolnnllmklapflnccelgolnpehhpl$"] → $workspace3

# }}}

# Startup {{{
exec_always --no-startup-id exec ~/.screenlayout/1x2x1.sh
exec_always --no-startup-id exec nitrogen --restore

exec --no-startup-id exec pulseaudio --start
exec --no-startup-id exec $HOME/Apps/TaskWarrior/Pomodoro/pomodoro-daemon.py
exec --no-startup-id exec autokey
exec --no-startup-id exec nm-applet
exec --no-startup-id exec clipit -n
exec --no-startup-id exec compton -f
exec --no-startup-id exec shutter --min_at_startup

exec --no-startup-id i3-msg 'workspace "3:   Slack"; layout stacking' 
#exec --no-startup-id i3-msg 'workspace "2:   Chrome"; exec chromium-browser;'
exec --no-startup-id i3-msg 'workspace "3:   Slack"; exec slack'
#exec --no-startup-id i3-msg 'workspace "4:   Terminals"; exec i3-sensible-terminal'

# }}}
